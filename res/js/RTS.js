
/**
 * Our main RTS application object.
 */
var RTS = klass(function(settings) {

  this.settings = _.extend({
    'game_speed' : 10
  }, settings);

});


RTS.methods({

  'start' : function() {
  }
});